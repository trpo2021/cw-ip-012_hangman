#pragma once

#include <string>
#include <vector>

using namespace std;

int word_check(
        string answer,
        char letter,
        vector<char>& found,
        vector<char>& entered_letters);